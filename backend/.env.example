# ===========================================
# PRODUCTION SECURITY CHECKLIST
# ===========================================
# Before deploying to production, ensure:
# 1. NODE_ENV is set to 'production'
# 2. JWT_SECRET is a secure random string (min 64 characters)
# 3. DATABASE_URL uses SSL connection
# 4. All sensitive values are set via environment variables
# 5. FRONTEND_URL is set to your actual domain(s)
# ===========================================

# Database
# For production, use SSL: ?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/crypto_mlm?sslmode=require"

# Server
PORT=5001
# IMPORTANT: Set to 'production' for live deployment
NODE_ENV=development

# JWT Configuration
# SECURITY: Generate a secure random string for production (min 64 characters)
# Use: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_64_CHARACTERS_LONG_USE_OPENSSL
JWT_EXPIRES_IN=7d

# Email (using Resend - https://resend.com)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxx
# For testing, use onboarding@resend.dev
# For production, verify your domain at https://resend.com/domains
EMAIL_FROM="Crypto MLM Platform <noreply@yourdomain.com>"

# Frontend URL (comma-separated for multiple domains)
# SECURITY: Only include your actual production domains
FRONTEND_URL=https://yourdomain.com,https://www.yourdomain.com

# Deposit Configuration
# Admin wallet for receiving deposits
ADMIN_DEPOSIT_WALLET=0x...your-eth-wallet-address

# Network and Token Configuration
# Options: 'Sepolia Testnet', 'Goerli Testnet', 'TRC20', 'Mainnet', etc.
# IMPORTANT: Use mainnet for production
DEPOSIT_NETWORK=Sepolia Testnet
DEPOSIT_TOKEN=ETH

# For production use TRC20 USDT:
# DEPOSIT_NETWORK=TRC20
# DEPOSIT_TOKEN=USDT
# ADMIN_DEPOSIT_WALLET=TRX...your-trc20-wallet-address

# Optional: Ethereum/USDT (for future features)
USDT_CONTRACT_ADDRESS=0xdAC17F958D2ee523a2206206994597C13D831ec7
NETWORK_RPC_URL=https://mainnet.infura.io/v3/your-infura-key
ADMIN_WALLET_ADDRESS=0x...
# SECURITY WARNING: Never commit private keys to version control
ADMIN_WALLET_PRIVATE_KEY=your-private-key

# Bonus Configuration
COMPANY_PROFIT_SHARE=0.40
INVESTOR_PROFIT_SHARE=0.60
